rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permit read and write for all application collections and documents
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
